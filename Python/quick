```python
categories = [
    {"id": 1, "name": "shapes", "parent_id":  None},
    {"id": 2, "name": "nature", "parent_id":  None},
    {"id": 3, "name": "flowers", "parent_id": 2},
    {"id": 4, "name": "trees", "parent_id": 2},
    {"id": 5, "name": "plants", "parent_id": 2},
    {"id": 7, "name": "triangles", "parent_id": 1},
    {"id": 8, "name": "circles", "parent_id": 1},
    {"id": 9, "name": "squares", "parent_id": 1},

]
```


### Making a tree

```python
def _build_category_tree(categories):
    children_map = defaultdict(list)

    # SETUP
    for cat in categories:
        children_map[cat.parent_id].append(cat)
   

    def dfs(parent_id):

        if parent_id not in children_map:
            return []
        return [
            {
                **cat.to_json(),
                "children": dfs(cat.id)
            }
            for cat in children_map[parent_id]
        ]

    # Top level categories exist in the None bucket
    return dfs(None)
```
